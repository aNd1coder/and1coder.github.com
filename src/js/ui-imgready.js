/*
 * Copyright (c) 2012, Mttang Inc. All rights reserved.
 * @author: aNd1coder
 * @update: $Id
 * @overview:图片头数据加载就绪事件 - 更快获取图片尺寸
 * @usage:
 */

(function ($) {
    $.fn.imgready = function (options) {

        //defaults
        $.fn.imgready.defaults = {
        };

        //extend
        var options = $.extend({}, $.fn.imgready.defaults, options);

        //logics
        this.each(function () {

        })
    };
})(jQuery);